const outputViewer = document.querySelector(".output");
const numbers = document.querySelectorAll(".number");

const operation_signs = document.querySelectorAll(".operation_sign");

export { outputViewer, numbers,operation_signs };
